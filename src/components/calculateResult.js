/*

- for pints  will include 4 nested arrays, each with object (type (pint to half pint, bottle, third, quarter) and typeresult with 25 values)
- 0 index means now and 25 means in 25hrs 
- maybe standardise same data structure for others, but use only one array element
*/

function calculateResult(data) {
  console.log(`Result is calculated, data is ${JSON.stringify(data)}`);
}

module.exports = calculateResult;
